

const checkDrivingEligibilityES5=function(perosn){

    if(person.age>18)
        console.log("Hey, "+ person.name + " you can drive ");
    else
        console.log("Hey, "+ person.name+" you should wait for "+(18-person.age)+" years to drive");

}

const checkDrivingEligibilityES2015=function(perosn){

    if(person.age>18)
        console.log(`Hey, ${person.name} you can drive `);
    else
        console.log(`Hey, ${person.name} you should wait for ${18-person.age} years to drive`);

}

